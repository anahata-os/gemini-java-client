/**
 * Provides classes for logging chat history to the file system.
 * <p>
 * This package contains the {@link uno.anahata.gemini.context.history.HistoryLogger},
 * which is responsible for writing a summary of every {@link uno.anahata.gemini.ChatMessage}
 * added to the context into a dedicated log file. This creates a persistent, human-readable
 * audit trail of the conversation for debugging and review purposes.
 */
package uno.anahata.gemini.context.history;
